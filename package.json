{
  "name": "micro-frontend-experiments",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "_format:prettier": "prettier --write './*.{json,js,jsx,ts,tsx,css,less,scss,md}'",
    "clean": "lerna run clean --stream --parallel",
    "build": "lerna run build --stream",
    "desktop": "cd ./packages/desktop && npm run launch",
    "format": "npm run _format:prettier && lerna run format --stream",
    "postinstall": "lerna bootstrap",
    "start": "lerna run start --stream --parallel"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "^2.2.1"
  }
}
