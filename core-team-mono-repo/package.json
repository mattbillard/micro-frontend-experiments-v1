{
  "name": "core-team-mono-repo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "lerna run clean --stream --parallel",
    "build": "lerna run build --stream",
    "format": "lerna run format --stream",
    "start": "lerna run start --stream --parallel",
    "serve": "lerna run serve --stream --parallel"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
